namespace Bonds.Core.Helpers
{
    public static class CustomStringComparer
    {
        public static bool IsSimilar(string s1, string s2, double ratio = 0.8)
        {
            //по пробелу и запятым делить
            return false;
        }
    }
}
